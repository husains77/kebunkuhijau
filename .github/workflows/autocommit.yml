name: Auto Commit

on:
  push:
    branches:
      - main

  schedule:
     - cron: '*/2 * * * *' # Jalankan setiap jam

jobs:
  auto_commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Modify Last Update
        run: |
          d=`date '+%Y-%m-%dT%H:%M:%SZ'`
          echo $d > LAST_UPDATED

      - name: Commit Changes
        run: |
          git config --local user.name "husains77"
          git config --local user.email "jamesandersonn70@gmail.com"
          git add -A

          arr[0]="🚀 Updated: Auto commit"
          arr[1]="🌟 Changed: Auto commit"
          arr[2]="🎉 Fixed: Auto commit"
          arr[3]="💫 Added: Auto commit"
          arr[4]="✨ Updated: Auto commit"
          arr[5]="🌠 Changed: Auto commit"
          arr[6]="🌌 Fixed: Auto commit"
          arr[7]="🌙 Added: Auto commit"
          arr[8]="🚀 Updated: Auto commit"
          arr[9]="🌟 Changed: Auto commit"

          rand=$[$RANDOM % ${#arr[@]}]

          git commit -m "${arr[$rand]}"

      - name: GitHub Push
        uses: ad-m/github-push-action@master
        with:
          force: true
          directory: '.'
          github_token: ${{ secrets.GITHUB_TOKEN }}
